version: "3.9"

services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    ports:
      - "7000:7000"

  frontend:
    build: ./forntend
    ports:
      - "3000:3000"

  db:
    # image: mysql:8
    image: postgres:15
    env_file:
      - ./backend/.env
    ports:
      # - "3306:3306"
      - "5432:5432"