version: "3.9"

services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    ports:
      - "7000:7000"

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"

  db:
    # image: postgres:8
    image: postgres:15
    env_file:
      - ./backend/.env
    ports:
      # - "3306:3306"
      - "5432:5432"